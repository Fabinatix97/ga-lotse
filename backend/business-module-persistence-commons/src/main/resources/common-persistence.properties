# https://github.com/spring-projects/spring-boot/issues/7107
spring.jpa.open-in-view=false

# Should only be enabled explicitly and per business module
spring.liquibase.enabled=false
# Disable anonymous liquibase analytics
# https://docs.liquibase.com/analytics/home.html
# https://github.com/liquibase/liquibase/pull/6516
spring.liquibase.analytics-enabled=false

spring.jpa.properties.hibernate.dialect=de.eshg.persistence.EshgPostgreSQLDialect

# Enable batched inserts and updates
spring.jpa.properties.hibernate.jdbc.batch_size=100
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true

spring.jpa.properties.hibernate.jdbc.fetch_size=100

spring.jpa.properties.hibernate.type.preferred_duration_jdbc_type=INTERVAL_SECOND
