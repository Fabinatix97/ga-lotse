worker_processes  auto;

error_log  /proc/self/fd/2 debug;
pid        /var/run/nginx.pid;

events {
  worker_connections  1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
  'status=$status '
  'body_bytes_sent=$body_bytes_sent '
  'http_referer=$http_referer '
  '"$http_user_agent" '
  'http_x_forwarded_for=$http_x_forwarded_for '
  'upstream_addr=$upstream_addr '
  'upstream_status=$upstream_status '
  'request_time=$request_time '
  'upstream_response_time=$upstream_response_time '
  'upstream_connect_time=$upstream_connect_time '
  'upstream_header_time=$upstream_header_time ';

  access_log  /proc/self/fd/2  main;

  sendfile        on;
  #tcp_nopush     on;

  # The default proxy buffer size of 4k/8k is not enough and leeds
  # to an internal server error in Nginx:
  # "upstream sent too big header while reading response header from upstream"
  proxy_buffer_size   128k;
  proxy_buffers   4 256k;
  proxy_busy_buffers_size   256k;

  keepalive_timeout  10;
  send_timeout       10;
  client_header_timeout  60;
  client_body_timeout    60;
  client_header_buffer_size 4k;
  client_max_body_size {{ .Values.backend.upload.maxFileUploadSizeInBytes }};
  server_tokens      off;

  #gzip  on;

  include /etc/nginx/conf.d/*.conf;
}
